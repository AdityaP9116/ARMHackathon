# Rust
/target/
**/target/
Cargo.lock

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.ipynb_checkpoints/

# Build / native
*.so
*.dylib
*.dll
*.o
*.a
build/

# Models / data (do not commit large weights or MRI data)
*.safetensors
*.pt
*.pth
*.ckpt
/data/
/models/
/checkpoints/

# Golden vectors: core cases ARE committed; large benchmark-shaped ones are
# regenerated on demand via `python tests/gen_golden.py --large`
tests/golden/large_*.npz

# Benchmark outputs
*.csv
*.log
/results/

# Wheel build outputs
python/dist/
wheel-test-env/

# Benchmark result files (JSONs are per-host; commit deliberately if
# they're headline numbers)
bench/results/

# OS / editor
.DS_Store
.idea/
.vscode/
